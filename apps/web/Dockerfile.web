FROM node:24-alpine

WORKDIR /app

RUN corepack enable pnpm && pnpm config set dangerouslyAllowAllBuilds true

COPY package.json pnpm-lock.yaml ./
RUN pnpm install --frozen-lockfile

COPY . .

EXPOSE 5173

CMD ["pnpm", "run", "dev", "--host", "0.0.0.0", "--port", "5173"]
